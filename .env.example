# Where the RentCar Django API is listening. The dev server proxies to it, which keeps the
# browser same-origin — the backend has no CORS middleware installed.
VITE_BACKEND_ORIGIN=http://127.0.0.1:8000
